<%= form_for(tour_keyword) do |f| %>
  <% if tour_keyword.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tour_keyword.errors.count, "error") %> prohibited this tour_keyword from being saved:</h2>

      <ul>
      <% tour_keyword.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :keywords %>
    <%= f.text_area :keywords %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
